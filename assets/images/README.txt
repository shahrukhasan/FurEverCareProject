FurEver Care local image assets
===============================
These image files are bundled locally and derived from the images in the supplied SRS PDF.
The website HTML, JavaScript fallback data, products JSON and adoption JSON use these files instead of remote photo URLs.
